'use client'

// The edit form already exposes the three underlying groups (verification,
// review, provenance); duplicating them as readonly summaries would only add
// noise. The list Cell carries the combined signal.
export const PublishReadinessField = () => {
  return null
}

export default PublishReadinessField
