'use client'

// Locale visibility lives entirely in the list Cell; the edit form has no
// extra information to show (the admin locale switcher is in the header).
export const LocaleField = () => {
  return null
}

export default LocaleField
