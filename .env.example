DATABASE_URL=postgres://cleverblog:cleverblog@127.0.0.1:5432/cleverblog
PAYLOAD_SECRET=replace-with-at-least-32-random-characters
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Redirect serving
# Optional. Defaults to the incoming request origin.
# Set only an exact origin (without /api or another path), e.g. https://cleverblog.pl.
REDIRECTS_API_ORIGIN=
